# Reglas de ProGuard/R8 para UPB Conecta.
# En esta fase preliminar (solo UI con datos simulados) no hay reglas
# especiales que agregar; se completará cuando se integre el backend real
# (serialización JSON, cliente HTTP, etc.).
